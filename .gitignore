# Secrets — never commit your real API key
.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/

# Persistent vector store (regenerated from your uploads)
chroma_db/
# Throwaway vector store the evaluation harness ingests into
eval/_chroma_eval/

# Gradio / temp
.gradio/
flagged/
